[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.26"
optimizer = true
optimizer_runs = 200
fuzz = { runs = 256 }

[profile.ci]
fuzz = { runs = 1000 }

# RPC + Explorer filled from env in Desktop
[rpc_endpoints]
mantle = "${MANTLE_RPC_URL}"
